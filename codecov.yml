flag_management:
  default_rules:
    carryforward: true
    statuses:
      - type: project
        target: auto
        threshold: 60%
  individual_flags:
    - name: fa-compression 
      paths: 
      - fa-compression
      statuses:
        - type: project
          target: 90%
